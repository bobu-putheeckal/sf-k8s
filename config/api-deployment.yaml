apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-depl
  namespace: default
  labels:
    app: api
spec:
  selector:
    matchLabels:
      app: api
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: bobput/sf-demo-api:d15a3470903b46ee229afd72f3515b5052e165aa
          env:
            - name: PORT
              value: "3000"
            - name: MONGO_USER
              value: "root"
            - name: MONGO_PWD
              value: "example"
            - name: MONGO_URL
              value: "mongo-service.default"
            - name: MONGO_PORT
              value: "27017"
        # - name: mongo
        #   image: mongo
        #   env:
        #     - name: MONGO_INITDB_ROOT_USERNAME
        #       value: root
        #     - name: MONGO_INITDB_ROOT_PASSWORD
        #       value: example
